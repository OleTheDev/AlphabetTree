const expect = require('chai').expect;
const index = require('./index');

describe('AlphabetTree tests', () => {
    describe('Tree testing', () => {
        let tree = new AlphabetTree();
    });
});